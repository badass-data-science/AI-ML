# --- LLM / AI ---
litellm>=1.80.0          # provider-agnostic LLM calls (Ollama, Claude, OpenAI, ...)
instructor>=1.15.0       # structured outputs via Pydantic
tenacity>=9.0.0          # retry with exponential backoff

# --- Data models ---
pydantic>=2.0.0

# --- Vault parsing ---
python-frontmatter>=1.1.0  # robust YAML-frontmatter/body split for vault markdown

# --- Rendering ---
jinja2>=3.1.0             # draft resume/cover-letter templating

# --- Observability ---
structlog>=26.0.0         # structured logging

# --- CLI ---
typer>=0.16.0

# --- Testing ---
pytest>=9.0.0
pytest-asyncio>=1.0.0

# --- Tracing (optional — install to enable) ---
# Langfuse: no extra package needed; set LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY
# Phoenix (local UI + OTEL):
#   pip install arize-phoenix openinference-instrumentation-litellm
